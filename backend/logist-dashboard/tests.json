{
  "tests": [
    {"name": "OPTIONS", "method": "OPTIONS", "path": "/", "expectedStatus": 200},
    {"name": "No token - unauthorized", "method": "GET", "path": "/?section=today", "expectedStatus": 401},
    {"name": "Invalid token - unauthorized", "method": "GET", "path": "/?section=today", "headers": {"Authorization": "Bearer invalid.token.here"}, "expectedStatus": 401}
  ]
}
